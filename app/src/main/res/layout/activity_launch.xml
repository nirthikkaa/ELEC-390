<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/launchRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:fitsSystemWindows="true">

    <TextView
        android:id="@+id/tvLaunchTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Theremin Gloves"
        android:textSize="28sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/tvLaunchSubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="Android MVP • BLE Dual Glove Demo"
        android:textSize="15sp" />

    <TextView
        android:id="@+id/tvLaunchHint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:gravity="center"
        android:text="Tap start to open the live BLE + audio screen.\nMain screen will auto-connect to gloves."
        android:textAlignment="center" />

    <Button
        android:id="@+id/btnTapToStart"
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="28dp"
        android:text="Tap to Start" />

</LinearLayout>